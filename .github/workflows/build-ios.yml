      - name: Install Dependencies & Prepare Assets
        run: |
          npm install
          mkdir -p www
          cp index.html www/
          npx cap add ios || true
          npx cap sync ios
